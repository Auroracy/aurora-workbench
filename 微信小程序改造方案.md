# Aurora 工作台 → 微信小程序：可行性评估与资质清单

> 结论日期：2026-09-19 · 依据微信官方文档（链接见文末）

## 一、先说结论

微信**小程序码**只能由微信官方接口生成，且对网站有硬性门槛。当前工作台是
`http://106.14.223.116:9000`（裸 IP + HTTP），**不满足任何一条**。三条可行路径：

| 路径 | 需要什么 | 改造量 | 成本 | 周期 | 能用小程序码 |
|---|---|---|---|---|---|
| **D. 二维码 + PWA**（已落地） | 无 | 已完成 | 0 | 已完成 | 否（用普通二维码，效果接近） |
| **B. web-view 壳小程序** | 个体户/企业主体 + 域名 + ICP 备案 + HTTPS | 极小（1 个页面） | 域名约 50 元/年 + 认证 300 元 | 备案 7~20 个工作日 | ✅ 是 |
| **A. 个人主体原生小程序** | 身份证 + 小程序备案 | **极大**（全部页面用 WXML/WXSS 重写） | 0 | 数周开发 | ✅ 是 |

**不建议走 A**：个人主体**不允许使用 web-view、不允许配置业务域名**（官方明文），意味着现有
740KB 单文件（含基金、英语、塑身、日记等模块）必须整段重写，且公众号侧的数据同步、
LocalStorage 数据模型、pyodide、图表全部要换实现。

**要走小程序，只有 B 这条路成本可接受**——因为 web-view 能直接复用现有 H5。

---

## 二、已落地：方案 D（二维码 + PWA）

零资质、零成本，扫码即用，手机可"添加到主屏幕"变成类 App 图标。

新增文件（已部署到服务器与 GitHub Pages）：

| 文件 | 作用 |
|---|---|
| `manifest.json` | 应用清单：名称、图标、独立窗口（standalone）、主题色、快捷方式 |
| `sw.js` | Service Worker：页面网络优先 + 离线兜底；`/api/*` 永不缓存 |
| `icon-192 / 512 / maskable-512.png` | 安卓/PWA 图标（青绿→蓝渐变 + 极光光环 + A） |
| `apple-touch-icon.png` | iOS 主屏图标 |
| `favicon-64.png` | 浏览器标签图标 |
| `qr-server.png` | ① 自托管服务器入口二维码 |
| `qr-pages.png` | ② GitHub Pages 备用入口二维码 |
| `qr-workbench-card.png` | 两个二维码的分享卡片（可打印/发微信） |

页面内已注入：`theme-color`、`apple-mobile-web-app-capable`、图标 link，以及
**「添加到主屏幕」引导条**（仅手机端、非独立窗口时出现一次，可关闭且不再打扰）。

**已知限制（诚实说明）**：

- 服务器是 `http://IP:9000`，属于**非安全上下文**。浏览器规定 Service Worker 只能在
  HTTPS 或 localhost 注册，所以**线上目前不会启用离线缓存**（代码已写好，配好 HTTPS 域名后自动生效）。
- iOS Safari 的「添加到主屏幕」在 http 下**依然可用**（不需要 SW），图标 + 全屏无地址栏都能拿到。
- 安卓 Chrome 的「安装应用」在 http 下不会出现（同样需要 HTTPS）。
- 微信扫 `http://IP` 可能弹风险提示，**用系统相机扫更稳**。

---

## 三、方案 B：web-view 壳小程序（推荐的小程序路径）

### 3.1 硬性资质清单

| # | 项目 | 具体要求 | 说明 |
|---|---|---|---|
| 1 | **主体类型** | 个体工商户 或 企业（**个人不可**） | 官方明文：内嵌网页能力不开放给个人类型账号 |
| 2 | 营业执照 | 个体户执照即可 | 个人无法配置业务域名，所以必须有执照 |
| 3 | 小程序账号 | mp.weixin.qq.com 注册（免费） | 邮箱须从未注册过公众平台 |
| 4 | 微信认证 | 300 元/年（或选"对公打款验证"免费） | 开通高级接口的前提 |
| 5 | **域名** | 已注册的域名，**不能是 IP** | 约 50 元/年；只支持字母/数字/`-` |
| 6 | **ICP 备案** | 必须完成，**新备案域名需 24 小时后才能配置** | 工信部要求，7~20 个工作日 |
| 7 | **HTTPS 证书** | 必须 https，证书链完整，TLS ≥ 1.2 | Let's Encrypt 免费，可自动续期 |
| 8 | 校验文件 | 微信给的 `MP_verify_xxx.txt` 放到域名**根目录**，可直接访问 | 不能有跳转、不能被改写 |
| 9 | **小程序备案** | 2023-09 起小程序上线前也需备案 | 与域名备案分开 |
| 10 | 服务端口 | 需 443（HTTPS 默认端口） | 微信不接受自定义端口，现有 9000 端口要加反代 |

### 3.2 落地步骤

1. 买域名 → 解析到 `106.14.223.116`
2. 服务器装 Nginx，反代到 `127.0.0.1:9000`，配 Let's Encrypt 证书
3. 域名 ICP 备案（等 7~20 个工作日）→ 备案满 24 小时后可用
4. 注册个体户小程序 + 微信认证
5. 后台「开发管理 → 开发设置 → 业务域名」添加域名，上传校验文件通过验证
6. 小程序代码：**一个页面**，`<web-view src="https://你的域名/aurora-workbench.html" />`
7. 上传代码 → 提交审核 → 发布
8. 用 `getUnlimitedQRCode` 接口生成**小程序码**（需 access_token，服务端调用）

### 3.3 改造量

小程序侧只需要 3 个文件（`app.json` / `index.wxml` / `index.js`），**现有 H5 一行不用改**。
注意：web-view 加载的页面必须是 HTTPS，页面内 `/api/*` 走同源 https 请求，不受业务域名限制影响。

### 3.4 成本与周期

- 现金：域名 50 元/年 + 微信认证 300 元/年 ≈ **350 元首年**
- 时间：**备案 7~20 个工作日**是主要瓶颈，代码工作 1~2 天可完成
- 若已有域名和备案，可压缩到几天

---

## 四、方案 A：个人主体小程序（不建议）

个人主体注册免费（身份证 + 人脸核身），可发布工具/查询类小程序，但：

- ❌ **不能用 web-view、不能配业务域名** → 现有 H5 无法复用
- ❌ 不能开通微信支付、类目仅限工具/查询等少数
- ⚠️ 上线前仍需完成**小程序备案**
- 工作量：把 740KB 单文件的全部页面用 WXML/WXSS 重写，含基金估值、英语闯关、
  塑身图表、日记热力图、pyodide 等，**数周起**

---

## 五、官方依据

- 业务域名（含"不开放给个人类型账号"原文）：
  https://developers.weixin.qq.com/miniprogram/dev/framework/ability/domain.html
- 域名管理（业务域名/服务器域名限制、ICP 备案、校验文件）：
  https://developers.weixin.qq.com/doc/oplatform/developers/basic_func/domain.html
- 获取不限制的小程序码 getUnlimitedQRCode：
  https://developers.weixin.qq.com/miniprogram/dev/server/API/qrcode-link/qr-code/api_getunlimitedqrcode.html
- 小程序码总览（接口仅能生成已发布小程序的码）：
  https://developers.weixin.qq.com/minigame/dev/guide/open-ability/qr-code.html

---

## 六、建议路线

1. **现在就能用** → 方案 D（已完成）：扫 `qr-workbench-card.png` 里的码，iOS 用
   「分享 → 添加到主屏幕」，安卓等配好 HTTPS 后即可「安装应用」。
2. **想彻底变成小程序** → 走方案 B：先买域名 + 备案（最耗时，先启动），
   同时我可以在服务器上把 Nginx + HTTPS 反代配好，备案一下来就能立刻验证 https 版。
3. 备案期间什么都不耽误——https 配好后，方案 D 的离线缓存与安卓安装能力会自动启用，
   已经能获得 90% 的小程序体验。
