# Aurora 工作台 · 自托管部署指南

工作台是一个**单文件静态 HTML**（`aurora-workbench.html`），并配套一个**零依赖的 Node 服务**（`server.js`）——它同时负责托管页面 + 当同源 CORS 代理（抓国内金价、东方财富财报/基金、科技资讯等），完全脱离 Cloudflare，自主可控。

需要用到 3 个文件（都在仓库根目录）：
- `aurora-workbench.html` —— 工作台主文件
- `server.js` —— 零依赖 Node 服务（托管 + 代理）
- `package.json` —— 启动脚本

---

## 一、把文件放到服务器

**方式 A：在服务器上直接 git 拉取（最省事）**
```bash
cd ~
git clone https://github.com/Auroracy/aurora-workbench.git aurora
cd aurora
```

**方式 B：本机 scp 上传**（把下面路径换成你的文件实际位置）
```bash
scp -P 22 aurora-workbench.html server.js package.json 用户名@服务器IP:~/aurora/
# 若目录不存在先建：ssh 用户名@服务器IP "mkdir -p ~/aurora"
```

---

## 二、安装 Node（服务器上还没有时）

**Ubuntu / Debian**
```bash
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**CentOS / RHEL**
```bash
curl -fsSL https://rpm.nodesource.com/setup_lts.x | sudo -E bash -
sudo yum install -y nodejs        # CentOS 7
# 或 sudo dnf install -y nodejs   # CentOS 8+/Rocky/Alma
```

**验证**
```bash
node -v      # 看到 v14 以上即可
```

---

## 三、启动服务

```bash
cd ~/aurora
node server.js
```
- 默认监听 **8080** 端口
- 想直接用 80 端口（访问时不带端口号）：`sudo PORT=80 node server.js`

看到 `listening on 0.0.0.0:8080` 就说明起来了。

---

## 四、后台常驻（三选一，推荐 pm2 或 systemd）

**方式 1：nohup（最简单）**
```bash
nohup node server.js > server.log 2>&1 &
```

**方式 2：pm2（推荐，崩溃自动重启）**
```bash
sudo npm install -g pm2
pm2 start server.js --name aurora
pm2 save
pm2 startup        # 按屏幕提示执行它给出的那条命令，实现开机自启
```

**方式 3：systemd（见文末附录）**

---

## 五、开放端口

云服务器**两个地方**都要放行 8080：
1. 云厂商控制台「安全组」入站规则放行 `TCP 8080`
2. 系统防火墙：
```bash
# Ubuntu（ufw）
sudo ufw allow 8080/tcp

# CentOS（firewalld）
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
```

---

## 六、验证

浏览器打开：`http://服务器IP:8080`

命令行自检（服务器上执行）：
```bash
curl http://127.0.0.1:8080/health
# 应返回 {"ok":true,...,"routes":[...]}

curl http://127.0.0.1:8080/api/sge
# 应返回国内金价 JSON，如 {"price":949.48,...}
```

打开工作台后，切到「市场瞭望」Tab，国内金价 / 伦敦金价 / 科技资讯 / 财报日历 都会自动通过同源 `/api/` 抓取，不再需要任何第三方代理。

---

## 七、（可选）绑定域名 + HTTPS

有域名的话，用 nginx 反代 + certbot 免费证书最省心：

```nginx
# /etc/nginx/conf.d/aurora.conf
server {
    listen 80;
    server_name 你的域名;
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

```bash
sudo nginx -t && sudo systemctl reload nginx
sudo apt-get install -y certbot python3-certbot-nginx   # Debian/Ubuntu
sudo certbot --nginx -d 你的域名                          # 一键申请并配置 HTTPS
```

之后用 `https://你的域名` 访问即可（记得在安全组放行 80/443）。

---

## 附录 A：systemd 开机自启服务

新建 `/etc/systemd/system/aurora.service`（把 `用户名` 和路径改成你的）：
```ini
[Unit]
Description=Aurora Workbench
After=network.target

[Service]
Type=simple
User=用户名
WorkingDirectory=/home/用户名/aurora
ExecStart=/usr/bin/node /home/用户名/aurora/server.js
Environment=PORT=8080
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now aurora
sudo systemctl status aurora      # 确认 active (running)
```

---

## 附录 B：服务器无法访问 GitHub 时的替代上传方式

阿里云/腾讯云等国内服务器访问 GitHub 经常超时或被限速（报错 `Failure when receiving data from the peer`）。这种情况下用以下任一方式上传文件：

**方式 1：从本机 scp 上传（最可靠，不依赖服务器访问 GitHub）**

1. 先 SSH 到服务器查公网 IP：
   ```bash
   ssh admin@iZuf61a1sgc9t3k6nmc06uZ
   curl ifconfig.me      # 或 curl ip.sb，记下输出（例：1.2.3.4）
   ```
2. 在本机 Mac 终端执行：
   ```bash
   cd ~/WorkBuddy/2026-07-26-23-49-28     # 仓库根目录
   scp -P 22 server.js package.json aurora-workbench.html admin@服务器公网IP:~/aurora/
   # 例：scp -P 22 server.js package.json aurora-workbench.html admin@1.2.3.4:~/aurora/
   # 若 SSH 改了端口：scp -P 端口号 ...
   ```

**方式 2：服务器能访问 raw.githubusercontent.com 时直接 curl 下载**（在服务器上执行）
```bash
mkdir -p ~/aurora && cd ~/aurora
B=https://raw.githubusercontent.com/Auroracy/aurora-workbench/main
curl -fSL "$B/server.js"              -o server.js
curl -fSL "$B/package.json"           -o package.json
curl -fSL "$B/aurora-workbench.html"  -o aurora-workbench.html
ls -la       # 确认 3 个文件都下载下来了
```
注意：很多服务器 `raw.githubusercontent.com` 也被一并拦截，这条仅作兜底。

**方式 3：本机 git bundle 打包 + scp 传包**（最彻底，不依赖服务器任何 GitHub 访问）

本机 Mac 执行：
```bash
git clone https://github.com/Auroracy/aurora-workbench.git _bund
git -C _bund bundle create aurora.bundle --all
scp -P 22 aurora.bundle admin@服务器公网IP:~/
```

服务器执行：
```bash
mkdir -p ~/aurora && cd ~/aurora
git clone ~/aurora.bundle .
rm ~/aurora.bundle
```

---

## 常见问题

- **浏览器打不开 / 超时**：先确认服务在跑（`systemctl status aurora` 或 `pm2 ls`），再确认安全组 + 系统防火墙都放行了端口。
- **金价 / 财报空白**：必须是通过 `http://服务器IP:8080`（或你的域名）打开页面，前端才会走同源 `/api/`。如果直接用 `file://` 双击打开 HTML，代理不生效（那是本地文件，没有服务器）。
- **基金估值某只抓不到**：天天基金偶尔限流，工作台内置「手动填净值」兜底，不影响其他功能。
- **改端口后访问不了**：记得同步改安全组、防火墙、nginx（如果用了反代）三处。
